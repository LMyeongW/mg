<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="app">
	<insert id="submitPost">
		insert into memberapp (employeeId, applselect, startDate, endDate, emergencyTell, reason)
		values(#{employeeId}, #{applselect}, #{startDate}, #{startDate}, #{emergencyTell}, #{reason})
	</insert>	
	
	<select id="submitlist" resultType="com.study.demo.vo.ApplicationVO">
		select
			*
		from
			memberapp
		<if test="member != null">
        	<![CDATA[ where  employeeId =  #{employeeId} ]]>        
        </if>
	</select>
</mapper>